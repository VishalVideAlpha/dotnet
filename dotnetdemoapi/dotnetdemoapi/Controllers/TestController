using Microsoft.AspNetCore.Mvc;

namespace dotnetdemoapi.Controllers
{
    [ApiController]
    [Route("[controller]")]
    public class TestController : ControllerBase
    {
        private static readonly string[] tests = new[]
        {
            "test1", "employee", "test2", "test3" 
        };

        private readonly ILogger<TestController> _logger;

        public TestController(ILogger<TestController> logger)
        {
            _logger = logger;
        }

        [HttpGet(Name = "GetTest")]
        public IEnumerable<TestController> Get()
        {
            return Enumerable.Range(1, 5).Select(index => new TestController
            {
                Date = DateOnly.FromDateTime(DateTime.Now.AddDays(index)),
                name = test[Random.Shared.Next(name.Length)]
            })
            .ToArray();
        }
    }
}
